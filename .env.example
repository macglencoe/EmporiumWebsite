# Server-only Neon connection used by migration and verification commands.
DATABASE_URL=postgresql://role:password@host/neondb?sslmode=require

# Optional: required only for automated preview-branch creation.
NEON_API_KEY=
NEON_PROJECT_ID=
NEON_PARENT_BRANCH_ID=
